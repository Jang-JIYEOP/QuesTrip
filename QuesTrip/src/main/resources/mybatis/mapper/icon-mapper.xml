<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="IconMapper">
  <select id="list" resultType="com.kh.questrip.icon.vo.IconVo">
	SELECT 
	  	NO
	  	, TITLE
	  	, PRICE
	  	, PHOTO
	  	, DEL_YN
	  FROM ICON 
	  WHERE 
	  	DEL_YN = 'N'
  </select>
  
  
  <select id="pageList" resultType="com.kh.questrip.icon.vo.IconVo">
	SELECT 
	  	NO
	  	, TITLE
	  	, PRICE
	  	, PHOTO
	  	, DEL_YN
	  FROM ICON 
	  WHERE 
	  	DEL_YN = 'N'
	OFFSET #{pageNo} ROWS FETCH NEXT #{limit} ROWS ONLY
  </select>
  
 
  
</mapper>