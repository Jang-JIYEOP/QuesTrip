<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardMapper">

  <insert id="write">
  	INSERT INTO COMMUNITY_BOARD
  	(
  		NO,
  		MEMBER_NO,
  		TITLE,
  		CONTENT
  	)
  	VALUES
  	(
  		SEQ_COMMUNITY_BOARD_NO.NEXTVAL
  		, #{memberNo}
  		, #{title}
  		, #{content}
  	);
  </insert>
  
  <select id="list" resultType="com.kh.questrip.board.vo.BoardVo">
  	SELECT
  		NO,
  		MEMBER_NO,
  		TITLE,
  		CONTENT,
  		ENROLL_DATE,
  		DEL_YN
  	FROM COMMUNITY_BOARD
  	WHERE DEL_YN = 'N'
  	ORDER BY NO DESC;
  </select>
</mapper>